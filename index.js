const median = require('./src/median');
const mean = require('./src/mean');
const mode = require('./src/mode');
const variance = require('./src/variance');


module.exports = { mean, median, mode, variance }